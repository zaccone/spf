# /etc/default/spfd-grpc — independent from the Postfix service configuration.
SPFD_DNS=127.0.0.1:5335
SPFD_LISTEN=127.0.0.1:50051
SPFD_RECEIVER=mail.example.com
SPFD_TIMEOUT=20s
SPFD_MAX_CHECKS=64
SPFD_MAX_CONNECTIONS=256
